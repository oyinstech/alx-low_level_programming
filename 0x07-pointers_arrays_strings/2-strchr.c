#include "main.h"
#include <stdio.h>

/**
 * _strchr - locates a character in a string
 * @s: string to search
 * @c: char to find
 * Return: a string
 */
char *_strchr(char *s, char c)
{
	int i, b;

	i = 0;

	while (s[i])
		i++;
	for (b = 0; b < a; b++)
		if (c == s[b])
			s += b;
	return (s);
	{
		return ('\0');
	}
}
